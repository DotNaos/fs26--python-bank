[project]
name = "fs26-python-bank"
version = "0.1.0"
description = "Scaffold for the FS26 Python Bank assignment."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = []

[tool.uv]
package = false
